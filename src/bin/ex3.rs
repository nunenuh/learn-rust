fn main(){
    let spaces = "  ";
    let spaces = spaces.len();
    println!("the value of spaces is : {spaces}");

    // let mut spaces = "  "
    // spaces = spaces.len();
    // println!("the value of spaces is : {spaces}");
}